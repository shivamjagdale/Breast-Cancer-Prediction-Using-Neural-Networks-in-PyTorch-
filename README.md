# Breast-Cancer-Prediction-Using-Neural-Networks-in-PyTorch-
Breast Cancer Prediction Using Neural Networks in PyTorch 
If you can't view the file for some reason, download the file. 

1) Import Pytorch Libraries 
2) Import regular libraries 
3) We need to configure wether it will run on cpu or gpu. Check and print the device. 
4) Import the breast cancer dataset
5) Split into X & Y
6) Train test split 
7) Scale the data
8) X_train and X_test are in numpy array conver them into pytorch tensors. Move it to cpu or gpu whatever available, do that to all 4
9) Now start building artificial Neural Network
10) Now create hyperparameters 
11) Now initialize the model by passing the paramters. 
12) Define the loss and optimizers 
13) Train the Neural Network and calculate the accuracy. 
14) Now print those accuracy and epoch for every loop.
15) Now model evaluation for training and testing
